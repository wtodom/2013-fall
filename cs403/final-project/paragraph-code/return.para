to test_assignment_with x:
	show "the function was passed:".
	show x.
	show "we'll call this 'x'.".
	show "incrementing x, then returning x + 1.".
	Set x to (x + 1).
	return (x + 1).
	#

show ^ test_assignment_with(5).
show "should have printed 7.".

to test_short_function:
	return 5.
	#

show ^ test_short_function().
show "should have printed 5.".