(define (integer i)
	(define (add i)

			)
		)
	(define (promote i)

			)
		)
	(define (demote i)

			)
		)
	(define (rank i)

			)
		)
	(define (toString i)

			)
		)
	(define (value i)

			)
		)
	this
	)

(define (real r)
	(define (add r)

			)
		)
	(define (promote r)

			)
		)
	(define (demote r)

			)
		)
	(define (rank r)

			)
		)
	(define (toString r)

			)
		)
	(define (value r)

			)
		)
	this
	)

(define (rational n d)
	(define (add r)

			)
		)
	(define (promote r)

			)
		)
	(define (demote r)

			)
		)
	(define (rank r)

			)
		)
	(define (toString r)

			)
		)
	(define (value r)

			)
		)
	this
	)

(define (complex realPart imaginaryPart)
	(define (add c)

			)
		)
	(define (promote c)

			)
		)
	(define (demote c)

			)
		)
	(define (rank c)

			)
		)
	(define (toString c)

			)
		)
	(define (value c)

			)
		)
	this
	)